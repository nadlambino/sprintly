APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:bTCUbEAyxEqHeZOu0rvlPFQyXSFWz5x4u1Js6Ztg6dE=
APP_DEBUG=true
APP_TIMEZONE=Asia/Manila
APP_URL=http://task-manager.test

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
APP_MAINTENANCE_STORE=database

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=task_manager
DB_USERNAME=root
DB_PASSWORD=root

SESSION_DRIVER=cookie
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=".task-manager.test"
SANCTUM_STATEFUL_DOMAINS="task-manager.test"

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=public
QUEUE_CONNECTION=database

CACHE_STORE=database
CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# This is to determine how many days old a trashed task should be before it is deleted.
# If set to 0, the trashed task will be deleted immediately when the next cron job runs.
# Set to 0 for immediate deletion for testing purposes.
DELETE_TRASH_DAYS_OLD=0

# This is to determine how often the cron job for deleting trash will run
# `daily` will run the cron job every day at midnight.
# `minute` will run the cron job every minute. This is useful for testing.
RUN_DELETE_EVERY=minute

VITE_APP_NAME="${APP_NAME}"
